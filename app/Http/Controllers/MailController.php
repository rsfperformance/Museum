<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class MailController extends Controller
{
    public function sendMail()
 {
    
//    $r = request();
//    $to_name = $r->name;
//    $to_email = $r->email;
//    $text = $r->message;
//    \Illuminate\Support\Facades\Mail::send("emails.mail", ['name' => $to_name], function 
//     ($message) use ($to_name, $to_email) {
//      $message->to($to_email, $to_name)->subject("Response Available");
//      $message->from("theboringsolution@gmail.com", "The Boring  
//      Solutions");
//    });
//    \Illuminate\Support\Facades\Mail::send("emails.mailtous", ['text' => $text, 'name' => 
//    $to_name, 'email' => $to_email, 'budget' => $r->budget], function
//    ($message) use ($to_name, $to_email) {
//    $message->to("theboringsolution@gmail.com", "The Boring Solutions")->subject("Response Available");
//     $message->from($to_email, $to_name);
//    });
}
}
